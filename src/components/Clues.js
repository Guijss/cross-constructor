import React from 'react';
import styled from 'styled-components';

const CluesWrapper = styled.div`
  position: relative;
  height: 80%;
  width: 10%;
  display: flex;
  justify-content: flex-start;
  align-items: center;
`;

const Clues = () => {
  return <CluesWrapper></CluesWrapper>;
};

export default Clues;
